services:
  backend:
    image: ${ECR_REGISTRY}/reachgtm-backend:${IMAGE_TAG}
    volumes: []
    # Reset the base file's dev --reload command back to the production CMD.
    command: ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "0.5"
    logging:
      driver: awslogs
      options:
        awslogs-group: /ecs/reachgtm-backend
        awslogs-region: us-east-1
        awslogs-stream-prefix: ecs

  agents:
    image: ${ECR_REGISTRY}/reachgtm-agents:${IMAGE_TAG}
    volumes: []
    # Reset the base file's dev --reload command back to the production CMD.
    command: ["uvicorn", "agents.app.main:app", "--host", "0.0.0.0", "--port", "8001"]
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "1.0"
    logging:
      driver: awslogs
      options:
        awslogs-group: /ecs/reachgtm-agents
        awslogs-region: us-east-1
        awslogs-stream-prefix: ecs

  frontend:
    image: ${ECR_REGISTRY}/reachgtm-frontend:${IMAGE_TAG}
    volumes: []
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.25"
    logging:
      driver: awslogs
      options:
        awslogs-group: /ecs/reachgtm-frontend
        awslogs-region: us-east-1
        awslogs-stream-prefix: ecs

  db:
    ports: []

  redis:
    ports: []
